kind: Service
metadata:
  name: backend-service
  namespace: staging
  labels:
    app: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
